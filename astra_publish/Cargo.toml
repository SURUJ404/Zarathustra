[package]
name = "astra_publish"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Verifier export targets for Zara circuits (EVM, Starknet, WASM, Aleo, JSON test vectors)."

[dependencies]