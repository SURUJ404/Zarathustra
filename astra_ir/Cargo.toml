[package]
name = "astra_ir"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Core IR for the Zara circuit language: AST, R1CS and constraint kinds. No proof-system crypto."

[dependencies]
bls12_381.workspace = true
ff.workspace = true
num-bigint.workspace = true
